// 6.	Stripping Names: Store a person’s name, and include some whitespace characters at the beginning and end of the name. Make sure you use each character combination, "\t" and "\n", at least once. Print the name once, so the whitespace around the name is displayed. Then print the name after striping the white spaces. &nbsp

const nameWithTab: string = "Shariq\tRaza";
const nameWithNewline: string = "Jane\nSmith";

console.log("Name with Tab Character:");
console.log(nameWithTab);

console.log("\nName with Newline Character:");
console.log(nameWithNewline);
